name: build-test

on:
  push:
    branches:
      - main
    paths-ignore:
      - ".vscode/**"
      - "**.md"
      - "LICENSE"
  pull_request:
    paths-ignore:
      - ".vscode/**"
      - "**.md"
      - "LICENSE"
  workflow_dispatch:

concurrency:
  group: >
    ${{ github.workflow }}
    ${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.runner }} / ${{ matrix.framework }}
    runs-on: ${{ matrix.runner }}-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false # Important
      matrix:
        runner: [ubuntu, windows, macos]
        framework: [azul, dioxus, druid, freya, iced, makepad, slint, tauri]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install GKT (Linux only)
        if: matrix.runner == 'ubuntu'
        run: |
          sudo apt update 
          sudo apt install -y libgtk-3-dev libsoup # pkg-config

      - name: Build ${{ matrix.framework }} example
        working-directory: examples/
        run: cargo build --bin ${{matrix.framework}}
